# AI 跑团项目当前架构审计报告

> 审计日期：2026-06-18  
> 审计方式：依据《AI跑团项目_Codex自动对照审计清单》进行静态代码审计；按用户要求未运行测试。  
> 结论等级：**C. 具备规则引擎雏形的 AI RPG 系统**

## 一、执行摘要

项目已经超过“纯 AI 聊天 Demo”阶段：具备剧情状态、D20 检定与重投、回合制战斗、结构化战斗事件、任务/信任、SQLite 与 JSON 存档、上下文裁剪、AI 工具调用和隐藏状态指令等能力。尤其是后端 `BattleEngine`、`DiceService` 和前端 `StoryCheckResult`，已经形成较清晰的规则对象。

但当前还不能评为“规则驱动的可扩展 AI RPG 框架”。最关键的原因是：**前端实际战斗页面与后端权威战斗引擎是两套并行实现**，且后端权威战斗 API 在主流程中尚未被调用；`GameState` 仍以开放字典为主，故事、商店、物品、选择行动和战斗也没有统一的 `GameAction → Resolver → StatePatch` 管线。AI 虽受到提示词、工具和指令白名单约束，但仍可通过状态指令直接影响金币、物品、HP、信任和区域，甚至参与机械性的优势判定。

综合评分：**2.8 / 5**。现阶段适合继续制作内容和验证玩法，但在扩展更多战斗、NPC、物品及长期存档前，应先解决双战斗引擎、统一状态和统一动作入口三个问题。

## 二、总体架构判断

| 等级 | 判断 | 证据 |
|---|---|---|
| A. 纯 AI 聊天 Demo | ✖ | 已有规则检定、战斗引擎、存档与任务状态，不属于纯文本聊天。 |
| B. 带状态的 AI 跑团 Demo | ✔ | 当前主流程符合这一层级，状态和 AI 叙事已经结合。 |
| C. 具备规则引擎雏形的 AI RPG | ✔ **最接近** | `backend/engine/battle_engine.py`、`dice_service.py`、`state_directives.py` 已构成规则引擎雏形。 |
| D. 规则驱动的可扩展 AI RPG 框架 | ✖ | 权威引擎未接入主 UI，状态/动作/补丁尚未统一，缺少完整事件回放和全局 schema 校验。 |

## 三、逐项审计

### 1. GameState 统一状态容器

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 存在统一状态类型 | ⚠ | `frontend/src/types/game.ts:87` 定义 `GameState`，包含文档、线索、旗标、任务、场景、装备、信任等；但同时保留 `[key: string]: any`，因此只是名义上的统一容器。后端仍普遍使用普通 `dict`。 |
| 所有模块引用同一状态结构 | ✖ | `App.tsx` 主状态、`BattleTestScreen.tsx` 本地战斗状态、后端 `BattleEngine` 状态、小游戏状态分别维护。 |
| 状态修改集中管理 | ✖ | `frontend/src/services/dndRuntime.ts:155` 有 `applyStateChange()`，但 `App.tsx` 中仍有大量直接 `setGameState` 和对象展开更新；`backend/api/routes_dnd.py:678` 还允许 `state.update(patch)`。 |
| 状态可序列化并存档 | ✔ | `backend/kp/memory.py:77-95` 建立状态与存档表；`backend/api/routes_dnd.py:710` 保存状态、剧情、建议、阶段、聊天和记忆。 |

字段完整性：

| 字段 | 状态 | 说明 |
|---|---|---|
| `sessionId` | ⚠ | API 有 `session_id`，但未作为强类型字段进入 `GameState`。 |
| `phase` | ⚠ | 主 UI 阶段、剧情阶段和战斗 phase 分离。 |
| `turn` | ⚠ | 战斗中有 `round/turnIndex`，没有全局统一回合。 |
| `scene` | ⚠ | `current_area`、`sceneState.currentScene`、节点 ID 等表达并存。 |
| `player` | ⚠ | 玩家属性既有扁平字段，也有局部对象表达。 |
| `party` | ⚠ | 招募标志、同伴状态和战斗角色未形成一个规范队伍模型。 |
| `enemies` | ⚠ | 主要存在于战斗配置和战斗状态，未纳入全局状态类型。 |
| `battle` | ⚠ | 后端结构完整，但与主 `GameState`、存档主流程脱节。 |
| `inventory` | ⚠ | 能保存，但核心表示仍以逗号分隔字符串为主，缺少强类型条目。 |
| `flags` | ✔ | `GameState.flags` 及多处剧情条件/补丁已使用。 |
| `logs` | ⚠ | 剧情、信任和战斗事件各自存在，没有统一事件日志。 |

结论：**⚠ 部分符合，2.5 / 5**。最大风险是状态别名和开放 `any` 会让后续剧情条件、迁移与调试越来越困难。

### 2. Action 统一动作系统

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 统一 `GameAction` | ✖ | `frontend/src/types/game.ts:6` 只有简单 `ActionSuggestion {id,label,text}`；移动、对话、物品、商店、检定、战斗技能没有共同联合类型。 |
| Action 含 id/type/actor/target/payload | ⚠ | 后端战斗动作使用 `{actorId, skillId, targetIds}`，结构良好，但仅限战斗。 |
| Action 进入统一解析入口 | ✖ | 剧情、选择行动、商店、背包和战斗各自处理。 |
| 统一产出 `ActionResult/StatePatch` | ✖ | 战斗返回结构化事件，剧情返回 SSE 文本与状态更新，前端选择行动返回 `StoryCheckResult`，结果协议不统一。 |
| UI 只 dispatch Action | ✖ | `App.tsx` 与 `BattleTestScreen.tsx` 同时负责交互、规则计算和状态更新。 |

已有可复用基础：`frontend/src/utils/selectionAction.ts:11` 的 `SelectionActionCheck` 已把所有“选择行动”检定抽成独立类，并负责解析 `【属性DC】`、重投、确认与锁定提示；这是未来统一动作解析器的良好起点。

结论：**⚠ 局部具备，2.0 / 5**。

### 3. 骰子系统

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 统一 DiceService | ⚠ | `backend/engine/dice_service.py` 支持 seed、固定骰、公式与日志；但 `BattleTestScreen.tsx:1387`、`battlePrep.ts:177`、`TestScreen.tsx:80` 等仍各自调用 `Math.random()`。 |
| D20 判定 | ✔ | 前后端均支持 D20 + 加值对 DC/防御。 |
| 多面骰与多骰 | ✔ | 后端 `roll_formula()` 支持 `NdM±K`；前端战斗支持伤害多骰并排显示。 |
| 伤害骰 | ✔ | 战斗技能命中后独立投伤害并结算。 |
| 优势/劣势 | ✔ | `backend/engine/rules_dnd.py` 有优势/劣势 D20；前端亦有双骰取高/低流程。 |
| 自然 1/20 | ✔ | 战斗命中与暴击逻辑可识别自然点数。 |
| 确定性回放 | ⚠ | 后端权威引擎保存 `rngSeed` 与 `diceEvents`，但当前主 UI 战斗未使用该引擎，实际流程无法依靠 seed 完整复现。 |
| 结果结构化 | ⚠ | 后端骰子事件结构化；前端 `DiceResult` 仍是 `type + Record<string, any>`。 |

严重风险：`frontend/src/components/BattleTestScreen.tsx:2934` 会请求 AI 判断优势/劣势，意味着 AI 能影响机械结算。优势状态应由规则、效果和场景标签确定，AI 只能解释原因。

结论：**✔ 能力完整但尚未统一，3.5 / 5**。

### 4. 剧情检定与重投

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 独立 Check 对象 | ✔ | `frontend/src/utils/battlePrep.ts:61` 的 `StoryCheckResult` 包含 check/action/属性/DC/修正/初骰/重投/最终骰/来源/确认状态。 |
| 初次检定只发生一次 | ✔ | `SelectionActionCheck` 保存同一个 `StoryCheckResult`，续写使用已锁定结果。 |
| 重投消耗资源 | ✔ | `battlePrep.ts:102-121` 迁移并扣减虚构骰子、万能骰子数量。 |
| 新旧骰并存 | ✔ | 数据层同时保留 `initialRoll` 和 `reroll`，UI 可并排展示。 |
| 最终结果明确 | ✔ | `finalRoll.source` 标识 initial/fiction/omni，`finalized` 阻止再次改判。 |
| AI 只引用最终结果 | ✔ | `selectionAction.ts:55` 生成锁定提示，明确禁止 AI 再投骰或改变成败。 |

残余风险：该流程仍借用了战前选择类型与字符串库存；自由文本聊天工具链还能自行触发检定，尚未全部收口到同一 `CheckResolver`。

结论：**✔ 当前完成度最高的子系统之一，4.0 / 5**。

### 5. 战斗系统

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 战斗阶段机 | ✔ | `backend/engine/battle_engine.py:17-24` 定义开始、先攻、回合开始、等待动作、校验、解析、回合结束、战斗结束。 |
| 当前行动者与回合推进 | ✔ | `battle_engine.py:291`、`:518` 管理当前角色、轮次、冷却和死亡跳过。 |
| 动作合法性验证 | ✔ | `battle_engine.py:312` 验证行动者、死亡/眩晕、技能、冷却和目标。 |
| 命中、伤害、治疗、状态 | ✔ | 支持 D20 命中、暴击、伤害、护甲、治疗、临时 HP、持续状态。 |
| 结构化战斗事件 | ✔ | 引擎产生 attack/damage/heal/status/turn 等事件，UI 可据此展示。 |
| 事件可完整回放 | ⚠ | 有事件和骰子日志，但事件缺少统一 eventId、动作来源链和稳定时间序列；也没有从事件重建战局的入口。 |
| 战斗状态持久化 | ✖ | `backend/api/routes_battles.py` 用进程内 `_BATTLES` 保存，服务重启即丢失，也未进入普通存档。 |
| 主流程使用权威引擎 | ✖ | `frontend/src/services/api.ts:471-492` 虽定义权威战斗 API，但项目内没有主流程调用；`BattleTestScreen.tsx` 仍自行处理骰子、HP、阶段和 AI。 |

结论：**⚠ 后端引擎本身接近 4/5，产品实际接入仅约 2/5，综合 3.0 / 5**。这是当前最高优先级架构问题。

### 6. LLM 与规则引擎隔离

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| LLM 不直接生成命中/伤害 | ⚠ | 提示词要求 AI 不替战斗做决定，但聊天工具可触发检定，前端还让 AI 判优势状态。 |
| LLM 不直接修改状态 | ⚠ | `backend/engine/state_directives.py:266-290` 通过白名单处理 `[CMD:*]`，比任意 JSON 安全；但 AI 仍可实际改变金币、物品、HP、信任和区域。 |
| LLM 主要负责叙事与意图 | ✔ | `prompt_builder_dnd.py` 明确 DM 叙事边界，战斗引擎负责机械解析。 |
| 结果经规则层验证 | ⚠ | 战斗动作有严格验证；剧情状态指令只验证指令种类，字段级约束和业务上限不足。 |

结论：**⚠ 基本隔离但边界仍有漏洞，3.0 / 5**。

### 7. LLM 输出结构与容错

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 主输出统一 JSON | ✖ | 主聊天使用流式自由文本，加 `[CMD:*]`、`[HINTS:*]` 协议，不是统一 JSON。 |
| Schema 验证 | ✖ | 未发现 Zod、JSON Schema 或 AJV；专项判断器使用 `json.loads`、范围裁剪和默认值。 |
| 非法输出 fallback | ✔ | `backend/api/routes_dnd.py:924-1003` 对流处理、状态应用和异常有兜底；多个专项 AI 判断器也有解析失败回退。 |
| StatePatch 可验证 | ⚠ | 指令名称有白名单，但 `/state/patch` 仍能对状态做宽泛更新。 |

结论：**⚠ 容错尚可，结构约束不足，2.5 / 5**。

### 8. UI 与规则解耦

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| UI 不直接计算规则 | ✖ | `BattleTestScreen.tsx` 约 3988 行，含随机数、命中、伤害、阶段、AI 调用和状态修改。 |
| UI 不直接改核心状态 | ✖ | `App.tsx` 约 5054 行，存在大量 `setGameState` 与局部补丁。 |
| UI 只渲染引擎结果 | ✖ | 部分面板是纯展示，但主剧情和战斗页面仍承担控制器与规则职责。 |
| 组件可复用 | ⚠ | 已拆出背包、角色信息、判定覆盖层、战前面板等组件，但业务状态仍与页面紧耦合。 |

结论：**✖ 解耦不足，1.5 / 5**。

### 9. 存档、读档与回放

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| GameState 可序列化 | ✔ | SQLite 与 JSON 均保存结构化状态。 |
| 存档含版本号 | ✔ | `backend/kp/memory.py` 保存 `schema_version: 1`。 |
| 读档恢复剧情上下文 | ✔ | `routes_dnd.py:750` 恢复状态、记忆、聊天、剧情和建议。 |
| 原子写入/损坏兜底 | ✔ | `memory.py:384-390` 先写临时文件再替换，读取异常有默认回退。 |
| 战斗中存档 | ✖ | 权威战斗对象只在进程内，未进入普通存档。 |
| 完整 Replay | ✖ | 没有统一 action/event 流、状态重建器与骰子回放入口。 |

结论：**存读档 4.0 / 5，回放 1.0 / 5，综合 3.0 / 5**。

### 10. 上下文压缩与长期记忆

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 最近对话窗口 | ✔ | `dm_service.py:309` 使用最近 20 条；路由亦限制历史窗口。 |
| 状态摘要 | ✔ | `prompt_builder_dnd.py:681-795` 压缩物品名、旗标、任务、状态和最近记忆。 |
| 场景摘要 | ⚠ | 有记忆片段与固定剧情上下文，但未发现持续更新、长度有界的标准 `SceneSummary`。 |
| 长期记忆 | ✔ | SQLite 保存 memories，并按相关性取回。 |
| Token 可控 | ⚠ | 主 DM 路径有明确窗口；个别同伴/专项函数仍直接序列化传入历史，缺少全局预算器。 |

结论：**⚠ 已有实用压缩，3.0 / 5**。

### 11. 物品系统

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 物品基本字段 | ⚠ | `InventoryPanel.tsx` 的本地定义含 id、名称、类别、数量、说明与效果文本，但不是共享领域模型。 |
| 类型分类 | ✔ | 已区分消耗品、装备、关键物品、文档与线索。 |
| 效果结构化 | ⚠ | 虚构骰子、万能骰子、治疗等有代码效果；多数效果仍靠名称和分支判断。 |
| 使用物品走统一 Action | ✖ | 背包、重投、商店和战斗各自处理。 |
| AI 可任意生成物品 | ⚠ | 指令类型受限，但 `add_item` 对具体物品名缺少目录校验。 |

结论：**⚠ 能用但扩展成本偏高，2.5 / 5**。

### 12. Flags、Quest 与剧情条件

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| Flags | ✔ | `GameState.flags` 与脚本场景条件广泛使用。 |
| Quest 状态 | ✔ | `questLog`、任务阶段和奖励流程已存在。 |
| 场景进入条件 | ✔ | `scriptedScenes` 与遭遇流程使用条件及 `statePatch` 控制。 |
| 奖励条件与重复保护 | ⚠ | 多数关键流程有旗标保护，但缺少统一、可审计的奖励事务机制。 |
| 命名唯一且一致 | ⚠ | 信任值存在 `se_trust/al_trust/sl_trust/kl_trust/trust_sl/...` 等新旧别名。 |

结论：**✔ 功能较完整，3.5 / 5**。

### 13. NPC 与关系系统

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 统一 NPCProfile | ⚠ | `frontend/src/data/characterRegistry.ts` 有角色 id、名称、类别、头像、别名；人格、目标、秘密、已知事实和说话风格散落在 prompt 与剧情文件。 |
| 关系/信任值 | ✔ | `companionTrust`、信任日志、记忆和专属支线均已实现。 |
| NPC 能读取关系状态 | ✔ | prompt builder 会注入信任与角色上下文。 |
| 数据唯一性 | ✖ | `characterRegistry.ts` 中发现重复的 `yunling` id，对应不同角色定义，存在覆盖或错误取角风险。 |

结论：**⚠ 关系系统成熟，角色数据模型仍需统一，3.0 / 5**。

### 14. 调试与开发工具

| 检查项 | 状态 | 证据与判断 |
|---|---|---|
| 剧情跳转/测试入口 | ✔ | `frontend/src/components/TestScreen.tsx` 提供剧情节点、骰子、战斗、小游戏、头像和背景入口。 |
| 日志系统 | ✔ | 后端有 logger 与日志目录。 |
| DebugTrace | ✖ | 未发现统一记录输入 Action、骰子、解析结果、状态前后差异、AI 原文和异常的追踪对象。 |
| 状态差异查看器 | ✖ | 没有通用 before/after diff UI。 |
| 回放与调试包导出 | ✖ | 没有 replay、事件重建或一键复制调试上下文。 |

结论：**⚠ 有测试入口，缺少系统级可观测性，2.0 / 5**。

## 四、已完成能力

- 完整的故事检定与两种资源重投流程，能够锁定最终骰并交给 AI 续写。
- 后端确定性骰子服务，支持公式、多骰、固定骰与骰子事件。
- 独立后端战斗阶段机，支持先攻、合法性验证、命中、伤害、治疗、状态和胜负。
- 剧情旗标、任务、同伴信任、关系记忆和条件场景。
- SQLite + JSON 存档，包含状态、剧情、建议、阶段、聊天与长期记忆。
- DM 上下文分层、近期窗口、状态压缩、记忆检索和输出兜底。
- 战斗、剧情节点、骰子、素材等开发测试入口。

## 五、部分实现能力

- `GameState` 已存在，但字段开放、别名多、前后端没有共享 schema。
- Action 在战斗和选择行动中有结构化对象，但未覆盖全游戏。
- DiceService 后端实现良好，主前端仍保留多处本地随机数。
- LLM 有工具、提示边界与指令白名单，但仍能影响部分机械结算和真实状态。
- 物品、NPC、任务都能工作，但数据定义分散在组件、剧情和提示词中。
- 战斗事件可供 UI 展示，但尚不能作为完整事件溯源或回放日志。

## 六、尚未实现能力

- 全游戏统一的 `GameAction`、`ActionResolver`、`ActionResult`、`StatePatch`。
- 单一权威战斗引擎接管实际游戏战斗。
- 对所有 LLM 结构化输出执行统一 schema 验证。
- 持久化战斗中状态，以及从动作/事件/seed 恢复战斗。
- 标准化 `SceneSummary` 与统一 token 预算器。
- 全局物品目录与可组合的 `ItemEffect` 类型。
- 完整 `NPCProfile` 数据模型及数据唯一性校验。
- DebugTrace、状态 diff、原始 AI 输出检查和调试包导出。

## 七、主要风险

1. **双战斗引擎漂移**：同一技能或状态在前端和后端可能产生不同结果；后续每次修改都要同步两套实现。
2. **状态不可控扩张**：`GameState[key: string]: any`、旧字段别名和宽泛 patch 会积累隐性兼容债务。
3. **AI 越过规则边界**：AI 优势判断和状态指令可能让叙事模型影响应由引擎决定的结果。
4. **战斗无法可靠恢复**：服务重启或战斗中读档无法恢复 `_BATTLES` 中的权威状态。
5. **大组件回归风险**：`App.tsx` 与 `BattleTestScreen.tsx` 同时承担 UI、规则、流程和网络职责，局部改动容易触发远处回归。
6. **数据标识冲突**：角色 ID 重复和信任字段别名可能导致取错角色、条件误判或存档迁移失败。

## 八、整改优先级

### P0：应先完成

1. **接通单一权威战斗引擎**：让 `BattleTestScreen` 只提交 `{actorId, skillId, targetIds}` 并消费后端事件；删除前端重复的命中、伤害、HP 和回合计算。
2. **建立版本化 Canonical GameState**：拆分 `session/story/player/party/battle/inventory/quests/flags/logs` 子状态，移除开放 `any`，为旧存档编写迁移器并统一信任/场景字段。
3. **建立统一动作和补丁管线**：引入 `GameAction` 联合类型、Resolver 注册表、`ActionResult` 与字段受限的 `StatePatch`；所有 UI 只 dispatch。
4. **收紧 AI 权限**：取消 AI 决定优势/劣势；AI 仅提交意图或候选补丁，由规则层验证库存、数值范围、场景权限和幂等标识后执行。
5. **持久化战斗状态**：把 battle state、action log、event log、dice log 和 seed 纳入普通存档。

### P1：随后完成

1. 统一前后端骰子入口，清除业务代码中的 `Math.random()`；所有骰子生成带 `rollId/source/seedIndex` 的结构化事件。
2. 为 AI 专项输出、SSE 事件和状态补丁增加统一 schema 校验及版本号。
3. 将 `App.tsx` 拆为应用状态控制器、场景控制器、动作分发器和纯展示组件；将 `BattleTestScreen.tsx` 拆成 battle adapter 与视图。
4. 建立全局 ItemCatalog、`ItemInstance` 与 `ItemEffect` 联合类型，禁止 AI 添加未登记物品。
5. 增加标准 `SceneSummary`，统一各 AI 调用的上下文预算。

### P2：体验与工程化增强

1. 建立完整 `NPCProfile`，合并散落的角色 prompt，并加入 ID 唯一性检查。
2. 增加 DebugTrace 面板：Action、骰子、AI 原文、解析结果、Patch、状态 diff 和异常一屏查看。
3. 实现基于 action/event/seed 的确定性 Replay 和调试包导出。
4. 为任务奖励、金币和物品发放增加事务 ID 与幂等保护。
5. 将剧情内容数据与流程代码进一步分离，支持独立内容包和版本迁移。

## 九、评分汇总

| 维度 | 得分 | 说明 |
|---|---:|---|
| 架构完整度 | 2.5 / 5 | 子系统齐全，但缺统一状态、动作和权威执行链。 |
| 稳定性 | 3.0 / 5 | 存档与兜底较好；双实现和大组件带来回归风险。 |
| LLM 可控性 | 3.0 / 5 | 有边界、工具和白名单；仍存在机械/状态越权入口。 |
| 可扩展性 | 2.5 / 5 | 能继续加内容，但每加一种动作、物品或战斗机制都会跨多处修改。 |
| 综合评分 | **2.8 / 5** | **规则引擎雏形已经成立，尚未形成单一、可验证、可回放的游戏内核。** |

## 十、300—500 字结论

目前项目已经具备一款可玩的 AI 跑团 RPG 所需的大部分功能面：剧情续写、D20 检定、资源重投、战斗、队友信任、任务旗标、背包、存读档和长期记忆都已有真实实现，而非仅停留在界面原型。后端战斗阶段机、确定性骰子服务和前端故事检定对象尤其值得保留，它们是后续工程化的核心资产。当前短板不是功能数量，而是这些能力尚未汇入同一条权威执行链：前端实战页面与后端战斗引擎重复计算，状态依赖开放字典和多个历史别名，剧情、物品、商店、选择行动与战斗各自拥有处理入口，AI 也仍能通过优势判断和隐藏指令触及机械结果。短期继续堆剧情和技能会让同步成本迅速上升。建议先用一个版本集中完成“统一 GameState、统一 GameAction/StatePatch、主 UI 接入后端权威战斗、战斗状态入档”四项 P0 工作；完成后，项目将从可玩的规则原型迈入可稳定扩展的框架阶段，新增章节、角色和技能的风险与成本都会明显下降。
