# 25条修改测试报告

> 测试日期：2026-06-20  
> 测试方法：代码静态审查 + 后端/前端逻辑对比验证

---

## 总体结论

| 状态 | 数量 |
|---|---|
| ✅ 完全生效 | 18 |
| ⚠️ 部分生效（有问题） | 4 |
| ❌ 未生效 / 新风险 | 3 |

**需要修：3条严重问题 + 4条残留问题**

---

## 逐条验证结果

### 1. 默认音量 BGM=50/SFX=100 + 左侧可随时调节

**状态：✅ 完全生效**

- `App.tsx:1578` — `bgmVolume` 默认 `0.5`（50%）
- `App.tsx:1579` — `sfxVolume` 默认 `1`（100%）
- `showAudioSettings` 控制左侧音频面板，可随时调节

---

### 2. 【】加值括号写明影响属性

**状态：✅ 完全生效**

- `scriptedScenes.ts:463-466` — 教学战斗 hints 正确显示 `【力量DC10】`、`【感知DC10】` 等
- 全局搜索确认所有 `【】` 格式均包含属性名

---

### 3. 教学战斗裂隙爬兽 HP=20

**状态：⚠️ 前端完成，后端未完成**

| 位置 | HP值 | 状态 |
|---|---|---|
| `battleConfigs.ts:417` (前端) | `hp: 20, maxHp: 20` | ✅ 已改 |
| `battle_engine.py:263,275` (后端) | `hp: 18, maxHp: 18` | ❌ 仍为18 |

**风险**：前后端不一致。如果后端有独立初始化逻辑，爬兽实际 HP 可能仍为 18，导致前端显示 20 但实际只有 18。需同步修改后端 `DEFAULT_UNITS` 中的爬兽数据。

---

### 4. +3 写"固定伤害"不写"修正"

**状态：✅ 完全生效**

- `DiceRollOverlay.tsx:401` — 统一使用 `'固定伤害'` / `'固定治疗'`
- 全局搜索 `修正` 无相关结果

---

### 5. 命中骰可重投（虚构骰/万能骰）

**状态：✅ 完全生效**

- `BattleTestScreen.tsx:1617-1622` — 战斗组件已支持 `rerollRoll` 比较逻辑
- `rerollUsed` 标志被追踪（`BattleTestScreen.tsx:1852`）

---

### 6. 原生20点→伤害/治疗翻倍

**状态：⚠️ 伤害翻倍完成，治疗翻倍未实现**

- `battle_engine.py:418` — `critical = roll == 20`
- `battle_engine.py:633` — `critical_multiplier = 2 if critical else 1`（仅伤害）
- `battle_engine.py:634` — `raw_damage = max(0, int(base_total * critical_multiplier * total_multiplier))`

**但是**：`_apply_healing`（第649行）**没有**接收 `critical` 参数，也**没有**翻倍逻辑。治疗骰的 nat20 不会触发双倍效果。

**前端显示**：`BattleTestScreen.tsx` 中治疗骰也未发现 nat20 双倍显示逻辑。用户要求的显示格式 `5（点数）+3（固定伤害）（大成功效果翻倍）=16` 在治疗骰上无法实现。

---

### 7. 移除"规则画像"

**状态：✅ 完全生效**

- 全局搜索 `规则画像` 在 `frontend/src/` 中 **0个匹配**

---

### 8. 每场战斗前检查 AI 连接

**状态：✅ 完全生效**

- `App.tsx:2353-2394` — `startDefaultGameWithAiCheck` 在游戏开始前调用 `checkAiHealth()`
- 不可用时弹出 `AiHealthModal` 阻止进入

---

### 9. 防御技能逻辑

**状态：✅ 逻辑正确**

- `battle_engine.py:623-631` — `damage_reduction_once` 状态在 `_apply_damage` 中消费
- 只有命中（`hit = True`）才会进入 `_apply_damage`，Miss 不会消费防御状态
- `sharedPartyBattleSkills.ts:3-16` — 前端描述：`下次受到伤害 -50%`

**验证通过**：敌方选中目标但未命中时，防御状态不会被消耗。

---

### 10. "回气"技能改为"为自己恢复"

**状态：✅ 完全生效**

- `sharedPartyBattleSkills.ts:21` — formula: `'恢复 1d8+3 HP'`, effect: `'立即恢复自身生命值。'`
- `BattleTestScreen.tsx:365` — 显示 `'为自己恢复 ${concise}点生命'`

---

### 11. 删除"查看吊箱封条"和"询问瑟琳"的 DC 判定

**状态：❌ 未完成 → 两个选项仍有 DC**

| 文件 | 行号 | 内容 | 状态 |
|---|---|---|---|
| `scriptedScenes.ts` | 554 | `"查看吊箱封条【智力DC10】"` | ❌ 仍有DC |
| `App.tsx` | 638 | `'查看吊箱封条【智力DC12】'` | ❌ 仍有DC |
| `scriptedScenes.ts` | 555 | `"询问瑟琳这些魔物为什么怕光"` | ✅ 已移除 |
| `App.tsx` | 639 | `'询问瑟琳这些魔物为什么怕光'` | ✅ 已移除 |

**额外问题**：两处"查看吊箱封条"的 DC 值不一致（DC10 vs DC12）。

---

### 12. 赫尔曼出场 BGM 重复触发

**状态：❌ 未完成**

- `GUILD_ARRIVAL` 场景（`scriptedScenes.ts:562`）**没有设置 `bgm` 属性**
- `resolveBgmTrack` 按区域回退：`冒险者公会` → `guildCompanions`
- 如果前一场景（temple）的 BGM 尚未停止，进入公会区域时会触发 BGM 切换，可能导致同名 BGM 重启

**修复方向**：给 `guild-arrival` 场景显式设置 `bgm: "/assets/bgm/bgm_03_guild_tavern_companions.mp3"`，避免 `resolveBgmTrack` 回退逻辑在场景中途再次触发同一 BGM。

---

### 13. 删除"说服米娜查看失踪远征队登记册"的 DC

**状态：✅ 基本完成**

- `scriptedScenes.ts:712` — `"说服米娜查看失踪远征队登记册"` **无 DC**
- `App.tsx:231` — `'说服米娜查看失踪远征队登记册'` **无 DC**
- `App.tsx:1112` — 公会阶段提示中同样**无 DC**

---

### 14. 快艇骰子重投不发生相同点数

**状态：⚠️ 无法确认**

- `TavernDicePoker.tsx` 中存在骰子重投逻辑，但未发现明确的"禁止重复点数"代码
- 如果当前实现依赖于随机重投（总是全部重投未保留的骰子），理论上点数可能重复
- 建议在重投逻辑中增加：每个保留的骰子面值不在下一次重投结果中出现

---

### 15. 艾琳问答期间 BGM 保持 + AI 续写不改 BGM

**状态：✅ 已完成**

- `cathedral-ailin-recruit` 和 `cathedral-ailin-recruit-finale` 都显式设置了 `bgm: "/assets/bgm/temple.mp3"`
- `resolveBgmTrack` 优先使用 `currentLine?.bgm`，所以 temple BGM 在整个场景中保持
- AI 续写不会覆盖已设置的场景 bgm

---

### 16. 同伴信任基础值=50

**状态：✅ 完全生效**

- `trust.ts:31-36` — `serin: 50, ailin: 50, brock: 50, kaiya: 50`
- 检查了 `scriptedScenes.ts` 中初始信任设置：艾琳入队时 `al_trust: 55`（略高于基值，合理），凯娅入队时 `kl_trust: 45`（略低于基值，合理偏差）

---

### 17. 艾琳某些判定成功→信任+2

**状态：✅ 完全生效**

- `App.tsx:2884-2901` — "请艾琳展示白枝修会巡礼经文" → trust +2
- `App.tsx:3602-3609` — "请求艾琳翻阅牺牲者遗录" → trust +2
- `App.tsx:3630+` — "让艾琳疗伤" 检定成功 → 回复至上限 + trust +2

---

### 18. 无 DC 判定的行动可重复选择

**状态：✅ 已修复**

- 有 DC 判定的"已完成"选项会被禁用
- 无 DC 判定（如"请艾琳展示白枝修会巡礼经文"）不会被 `used.has(key)` 检测拦截
- 验证通过：`constrainActionSuggestions` 中只有 DC 选项才进入 `used` 集合

---

### 19. 删除"已完成 3/3 次，下一次将推进剧情"

**状态：✅ 完全生效**

- 全局搜索 `已完成.*次.*下一次将推进剧情` → **0个匹配**

---

### 20. 取消行动次数上限=3的限制

**状态：❌ 未完成**

- `App.tsx:227` — `const STORY_NODE_CHOICE_LIMIT = 3;` **仍然存在**
- `App.tsx:917` — `choiceLimitForStage` 默认返回 `STORY_NODE_CHOICE_LIMIT`
- 只有 `PostBlueShoal` 节点（`isUnlimitedPostBlueShoalStage`）返回 `Number.MAX_SAFE_INTEGER`
- 非 PBH 阶段的节点（如布洛克招募阶段）仍受 **3次限制**

**这就是布洛克阶段出现"第2、3个选项不能选"的原因。**

---

### 21. 删除"和瑟琳确认米娜给出的暗号"选项

**状态：✅ 完全生效**

- 全局搜索 `和瑟琳确认米娜给出的暗号` → **0个匹配**
- `brock-recruited` hints 中已无此选项

---

### 22. 暗号提示框 UI

**状态：✅ 已完成**

- `App.tsx:5020-5043` — 暗号提示使用专用 UI 结构
- 包含标题、说明文字、"知道了"按钮

---

### 23. 奥兰抽奖 50G/次，18-20算成功

**状态：✅ 完全生效**

- `blackMarketDrawFlow.ts:35` — `ORLAN_DRAW_COST = 50`
- `blackMarketDrawFlow.ts:37` — `ORLAN_DIAMOND_MIN_ROLL = 18`

---

### 24. 云苓场景背景不切换

**状态：⚠️ 第 1528 行仍有 bgImage 切换**

- `scriptedScenes.ts:1528` — 云苓台词行内仍设置了 `bgImage: "/assets/scenes/12.webp"`
- 如果用户要求该行**不改背景**，需移除此行的 `bgImage` 设置

---

### 25. 商店价格调整

**状态：✅ 完全生效**

| 物品 | 原价 | 现价 | 状态 |
|---|---|---|---|
| 净化之心 | 120G | 250G | ✅ |
| 力量/智力/敏捷/体质/感知/魅力药水 | 30G | 100G | ✅ |
| 治疗药水 | 25G | 75G | ✅ |
| 虚构骰子 | 50G | 不变 | ✅ |
| 万能骰子 | 100G | 不变 | ✅ |

净化之心已置顶（排在数组第一位）。

---

### 26. 签约场景只显示输入框

**状态：✅ 完全生效**

- `App.tsx:2559` — `contract_signature_pending` 时只显示输入
- `App.tsx:861` — `getActionChoiceStage` 返回 null（无 hints）
- 验证通过：签约时不显示公会调查选项

---

### 27. guild-final-registration 后 hints 包含"前往降渊缆梯"

**状态：✅ 完全生效**

- `scriptedScenes.ts:1757` — 第一个 hint 为 `"前往降渊缆梯中枢"`

**但**：`forcedSceneForChoiceStage` 中只有 `guild_intel` 阶段有保护（不自动跳转）。如果这里的调查次数也用完了，会不会自动跳？需确认。

---

### 28. 移除 hint 数量上限（4选项限制）

**状态：✅ 已无限制**

- 全局搜索未发现将 hints 硬截断为 4 个的逻辑
- 现有的限制是**行动次数**（`choiceLimitForStage`），而非 hints 显示数量

---

## 新风险点汇总

| # | 风险 | 严重度 | 影响 |
|---|---|---|---|
| R1 | **后端爬兽 HP=18 未同步前端=20** | 🔴 高 | 战斗难度不一致，玩家体验分裂 |
| R2 | **治疗骰 nat20 不翻倍** | 🔴 高 | 用户明确要求"伤害、治疗翻倍"，治疗未实现 |
| R3 | **STORY_NODE_CHOICE_LIMIT=3 未取消** | 🔴 高 | 布洛克招募等普通节点仍受3次限制 |
| R4 | "查看吊箱封条"DC未移除 + DC不一致 | 🟡 中 | 两处仍带DC且DC值不同(10 vs 12) |
| R5 | 赫尔曼出场 BGM 可能重启 | 🟡 中 | 公会场景无显式bgm，回退逻辑可能触发重启 |
| R6 | 云苓场景行内 bgImage 切换 | 🟡 中 | 可能仍会切换背景 |
| R7 | 快艇骰子无防重复点数逻辑 | 🟢 低 | 用户要求"点数一定会变" |
| R8 | 前后端 HP 数据架构不统一 | 🟡 中 | 前端 battleConfigs.ts 和后端 battle_engine.py 各自维护 |

---

## 建议修复优先级

### 立即修复（影响游戏核心体验）

1. **R1** — 后端爬兽 HP 改为 20（`battle_engine.py:263,275`）
2. **R2** — 治疗骰添加 nat20 双倍逻辑（`battle_engine.py:_apply_healing`）
3. **R3** — 将 `STORY_NODE_CHOICE_LIMIT` 改为 `Number.MAX_SAFE_INTEGER` 或大幅提高

### 尽快修复（影响特定场景体验）

4. **R4** — 移除两处"查看吊箱封条"的 DC
5. **R5** — 给 `guild-arrival` 场景添加显式 bgm
6. **R6** — 移除云苓场景中 line 1528 的 bgImage 切换

### 后续优化

7. **R7** — 快艇骰子增加防重复逻辑
8. **R8** — 考虑前后端数据统一（长期架构优化）

---

*报告完毕。*
